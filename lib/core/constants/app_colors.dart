import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors (White Theme)
  static const Color lightBackground = Colors.white;
  static const Color lightPrimary = Colors.black;
  static const Color lightText = Colors.black;
  static const Color lightButton = Colors.black;

  // Dark Theme Colors (Black Theme)
  static const Color darkBackground = Colors.black;
  static const Color darkPrimary = Colors.white;
  static const Color darkText = Colors.white;
  static const Color darkButton = Colors.white;
}
